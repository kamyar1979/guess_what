[project]
name = "guess-what"
source="src"
version = "0.1.0"
requires-python = ">=3.9"
dependencies = [
    "inflection>=0.4.0",
    "psycopg2-binary>=2.9.12",
    "aiopg>=1.4.0"
]

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]